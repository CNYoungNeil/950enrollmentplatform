// Shared course components — safe for both instructor and student to import.
// Instructor-only components (CreateCourseModal, SectionList, SectionFormModal,
// SectionFilesDrawer) are consumed directly by instructor pages and are not
// exported here.
export { default as CourseCard } from './CourseCard';
export { default as CourseStatusBadge } from './CourseStatusBadge';
