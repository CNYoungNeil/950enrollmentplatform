# Enrollment Platform

This repository is organized as a simple monorepo with separate frontend and backend directories.

## Structure

- `frontend/` front-end project placeholder
- `backend/` back-end project source code

## Notes

- The backend project structure and dependency list are documented in `backend/README.md`
